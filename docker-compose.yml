# Define the global project name
name: support-system-coding-test

services:
  # Laravel Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: support-system-app:latest
    container_name: support-system-app
    restart: unless-stopped
    tty: true
    volumes:
      - .:/var/www
    networks:
      - support-network

  # Nginx Web Server
  webserver:
    image: nginx:alpine
    container_name: support-system-webserver
    restart: unless-stopped
    ports:
      - "8001:80"
    volumes:
      - .:/var/www
      - ./docker-config/nginx:/etc/nginx/conf.d
    networks:
      - support-network
    depends_on:
      - app

  # MySQL Database
  db:
    image: mysql:8.0
    container_name: support-system-db
    restart: unless-stopped
    tty: true
    ports:
      - "3308:3306"
    environment:
      MYSQL_DATABASE: support_system_db
      MYSQL_ROOT_PASSWORD: root_password_here
      MYSQL_USER: support_user
      MYSQL_PASSWORD: root
    volumes:
      - support_dbdata:/var/lib/mysql
    networks:
      - support-network

networks:
  support-network:
    driver: bridge

volumes:
  support_dbdata:
    driver: local
